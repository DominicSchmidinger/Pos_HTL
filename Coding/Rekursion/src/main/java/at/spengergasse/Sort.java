package at.spengergasse;

import java.util.ArrayList;
import java.util.List;

public class Sort {

    // Das 2D-Array (von außen befüllt)
    private boolean[][] gebiet;

    public Sort(boolean[][] input) {
        this.gebiet = input;
    }

    public int findeGroesstesGebiet() {
        int maxGroesse = 0;

        for (int r = 0; r < gebiet.length; r++){
            for (int c = 0; c < gebiet[0].length; c++){

            }
        }
    }
}
